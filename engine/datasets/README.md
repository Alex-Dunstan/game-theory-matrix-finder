# Generated datasets

Dataset CSVs are not stored in Git. Generate them locally with:

```bash
cd engine
python generate_datasets.py
python enrich_datasets.py
```

Raw files are written here; enriched files are written to `enriched/`.
